{%extends "base.html.twig"%}

{% block contenu %}
  <br><center><h3>Voici la liste des entreprises proposant un stage</h3><br><br></center>

<div class="card-columns">
  {% for entrepriseCour in entreprises %}
    <div class="card">
      <a href="" class="card-link">
        <img class="card-img-top" src="{{entrepriseCour.URL}}" alt="Card image cap">
      </a>
      <div class="card-body">
        <a href="{{path('pro_stage_stagesParEntreprise',{'nom': entrepriseCour.intitule})}}">
            <h5 class="card-title">{{entrepriseCour.intitule}}</h5>
            <p class="card-text"><b>Activité : </b>{{entrepriseCour.activite}}</p>
            <p class="card-text"><b>Adresse : </b>{{entrepriseCour.adresse}}</p>
        </a>
        <a class="nav-link enable" href="{{path('pro_stage_modifEntreprise',{'id':entrepriseCour.id})}}">
          <p class="card-text"><b>Modifier</b></p>
        </a>
      </div>
    </div>
  {% endfor %}
  </div>

  <nav aria-label="Page navigation example">
    <ul class="pagination justify-content-center">
      <li class="page-item"><a class="page-link" href="#">Previous</a></li>
      <li class="page-item"><a class="page-link" href="#">1</a></li>
      <li class="page-item"><a class="page-link" href="#">2</a></li>
      <li class="page-item"><a class="page-link" href="#">3</a></li>
      <li class="page-item"><a class="page-link" href="#">Next</a></li>
    </ul>
  </nav>
{% endblock %}
