{% extends 'base.html.twig' %}

{% block title %}Stage{% endblock %}

{% block contenu %}
    <p></p><h1>Stage</h1><p></p>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Initule</th>
                <td>{{ stage.initule }}</td>
            </tr>
            <tr>
                <th>Descriptif</th>
                <td>{{ stage.descriptif }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ stage.domaine }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ stage.email }}</td>
            </tr>
            <tr>
                <th>URL</th>
                <td>{{ stage.URL }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stage_index') }}">Revenir en arrière</a>

    <a href="{{ path('stage_edit', {'id': stage.id}) }}">Editer</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
