{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block contenu %}
    <br><h1>Formation</h1><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ formation.intitule }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ formation.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ formation.telephone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ formation.mail }}</td>
            </tr>
        </tbody>
    </table>

    <br><a href="{{ path('formation_index') }}">Retour à la liste</a><br>

    <br><a href="{{ path('formation_edit', {'id': formation.id}) }}">Editer</a><br>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
